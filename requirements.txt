# 핵심 기능(filerenew 패키지)과 GUI는 파이썬 표준 라이브러리만으로 동작하므로
# 필수 의존성은 없습니다.
#
# 아래는 선택 사항입니다.
#
# 드래그 앤 드롭으로 파일을 추가하려면 설치하세요. 없으면 '파일 추가…'
# 버튼 방식으로 자동 폴백합니다.
tkinterdnd2>=0.4.0

# Windows .exe 빌드용(개발 시에만 필요)
# pyinstaller>=6.0
